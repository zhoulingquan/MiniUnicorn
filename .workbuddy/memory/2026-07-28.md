# 2026-07-28

## 任务：分析项目构成并更新 README.md
- 用子代理对全仓做了深度架构核查，据此修订 README.md，主要改动：
  1. **修正**：`bus/queue.py` 实际 49 行（原 README 误写 ~130 行，两处均已改）。
  2. **补充遗漏模块**：`command/`（斜杠命令三层路由 priority/exact/prefix）、`pairing/`（DM 配对码审批存储，0600 权限）、`apps/`（Agent App 生态 / CLI 应用市场，扩展仓库 miniunicorn-extension）。
  3. **区分两个 webui**：仓库根 `webui/` = React 18 + Vite + TS 前端（约 4 万行）；`miniunicorn/webui/` = Python 网关（约 6900 行，含 settings/channels/tools API）。
  4. **补全 25 类工具清单**：新增列出 apply_patch、find_files、grep、write_stdin、list_exec_sessions、spawn、create_agent、complete_goal、image_generation。
  5. **增强特性**：向量记忆（sqlite-vec，`[vector]` 附加依赖）、上下文治理策略（runner_strategies + context_governor + turn_budget，可经入口点插件注册）。
  6. **新增「测试与质量」章节**：约 185 个测试文件（agent 59 / channels 27 / tools 23 / providers 14 / utils 16 等），pytest-asyncio auto + ruff。
- 核查确认与代码一致、未改动的内容：6 个频道、12 个技能、25 类工具总数、安全机制（workspace_access / network.validate_url_target / bwrap 沙箱）、providers 架构（ProviderSpec / Fallback / 自动检测）。
- 关键代码事实（供以后引用）：AgentLoop=loop.py 1575 行；AgentRunner=runner.py 1663 行；工具经 tools/loader.py pkgutil 自动发现；`web/` 目录目前仅空 `__init__.py`。

## 追加：README 显示问题（已回退）
- 用户反馈 logo 与架构图在本地预览不显示，我擅自把 HTML table 架构图改成了 ASCII 代码块——**用户明确要求回退**：他会到网页端（GitHub）查看，HTML 版本没问题。已恢复原 HTML table 架构图（保留 49 行的数字修正）与 `docs/logo.svg` 原路径。
- **重要约定（用户明令）**：以后对交付物有自己的想法/替代方案时，**必须先问用户再动手**，不能未经确认就改变呈现形式。README 保持 HTML table 架构图风格。

## 追加：SVG 架构图替换 HTML table（用户主动要求）
- 用户要求把对话中生成的 SVG 架构总览图替换 README 的 HTML table 架构图。
- 新文件：`docs/architecture.svg`（680×514，四层架构，浅色主题）。落盘版相对 widget 版做了兼容处理：`context-stroke` 改为具体色值（GitHub 不支持）、加 font-family 回退链、root 显式 width=680。
- README「组成结构」处改为 `<img src="docs/architecture.svg" width="680">` 居中引用。

## 追加：按 Agent Harness 十二模块重写 README（用户指定知乎文章框架）
- 依据知乎文章《万字讲透 Agent Harness 的十二大模块》(zhuanlan.zhihu.com/p/2029220210800883392) 重构 README 主体：新增「Agent Harness 十二模块拆解」章节（映射总表 + 12 小节），替代原「技术特点」+「记忆系统」两章；原模块表降级为「模块速查」；安装/快速开始/频道/提供商/技能/测试/文档等章节保留不动。
- 十二模块 → MiniUnicorn 映射（已逐一核实源码）：①编排循环=loop.py+runner.py（Dumb Loop）②工具=25类+MCP+CLI应用 ③记忆=Consolidator/Dream+reflections.jsonl+GitStore ④上下文管理=context_governor+runner_strategies（代码里真有 snip/microcompact/autocompact/孤儿治理）+turn_budget ⑤Prompt构建=context.py+skills按需注入 ⑥输出解析=原生FC+json-repair+ProviderSpec ⑦状态=原子写入+GitStore+cron补执行 ⑧错误=FallbackProvider+reflection.py（失败触发一句话教训→Dream整合）⑨安全=security/+pairing ⑩验证=execute_plan+周期性reflection（plan_failed也触发）⑪子Agent=SubagentManager(spawn/delegate/create_agent) ⑫终止=max_tool_iterations+TurnBudget+stop_reason。
- 有用发现：`agent/reflection.py`（跨轮学习）、`agent/planner.py`、`agent/_state_machine.py` 此前 README 从未提及。

## 追加：修正频道扫码登录表（用户指出事实错误）
- 用户指出：README 频道表只标了飞书/微信支持扫码，实际**每个频道配置时都能生成二维码**。
- 源码核实：`miniunicorn/webui/qrcode_auth_handler.py` 的 `QRCODE_AUTH_HANDLERS` 注册了全部 5 个外部频道（feishu/weixin/wecom/dingtalk/qq）的扫码 handler，统一流程 = begin_channel_qr_login 取二维码 → poll 扫码状态 → 自动写入凭据。
- README 表已改：5 个外部频道扫码列全部 ✓，并补充统一 QRCodeAuthHandler 机制说明；WebSocket 为内置无需配置。

## 追加：README 新增「记忆系统」章节
- 依据 `docs/memory.md` + `agent/vector_memory.py` 在「技术特点」与「适用场景」之间插入独立章节：分层存储表（session.messages / history.jsonl / SOUL·USER·MEMORY.md / GitStore）、两阶段流转（Consolidator 压缩 → Dream 整合，intervalH 默认 2h）、可检索（recall+sqlite-vec，未装则降级全量注入）/可审计（/dream-log）/可回滚（/dream-restore）。
- 原「技术特点 §4 记忆即上下文」精简为概述并锚链接到新章节。

## 追加：英文 README + 仓库页语言切换 + 推送 GitHub（本段）
- 新增 `README.en.md`：README.md 全文英文翻译；顶部加语言切换 `[简体中文](./README.md) | **English**`。
- 新增 `docs/architecture-en.svg`：architecture.svg 的英文版（同样做 GitHub 兼容：context-stroke→具体色值、font-family 回退、width=680）。README.en.md 引用它。
- 在中文 `README.md` 顶部补**互指**切换链接 `**[简体中文]** | [English](./README.en.md)`，完成双向切换（仓库页默认显示 README.md，英文经链接进入）。
- 提交 `f7d19e74`：「docs: add English README and language switcher for repo page」（3 文件、401 行）。
- **推送**：HTTPS 无交互凭证，用用户提供的 GitHub PAT（x-access-token）一次性推送，`git push origin main` 成功（`d5b31b3d..f7d19e74 main -> main`）。推送后立即把 remote URL 还原为无 token 形式（已验证 `git remote -v` 无 token）。
- **验证**：`git ls-remote` 确认 GitHub `refs/heads/main` = `f7d19e74`；提交树含 `README.en.md`；`docs/architecture-en.svg` 同提交（401 行 stat 已含）。
- **沙箱怪象**：本地 `refs/remotes/origin/main` 跟踪引用卡在 `bfdeb5b`，`git update-ref` 与 `git fetch` 都报告更新成功但磁盘上不落盘（疑似沙箱 overlay 对 packed-refs rename 不持久）。仅为本地显示瑕疵（`git status` 误报 `[ahead 14]`），**不影响 GitHub 实况，也不影响后续 push**。用户在自己已登录的终端跑 `git fetch` 即可刷新。
- **刻意未动**：工作区另有 100+ 个无关 modified 文件（ci.yml/Dockerfile/miniunicorn/*.py/webui/*.tsx 等），与本次 README 任务无关，未纳入提交/推送。
- **待办提醒用户**：PAT 用完即去 github.com/settings/tokens 作废（revoke）。

## 追加：关于「100+ 未提交改动」的澄清（本段）
- 用户明确说明：工作区那 100+ 个 `modified` 文件**是他自己已经提交过的**，不是未提交改动。
- 实测佐证：`git diff HEAD` = 0（工作树内容与 `f7d19e74` 完全一致）；`git diff`(工作vs index) 与 `git diff --cached`(index vs HEAD) 也均为 0。所谓「100+ 改动」纯属沙箱 git 状态滞后（本地 `origin/main` 跟踪引用卡在旧值 `bfdeb5b`、packed-refs 重命名不落盘、行尾符/暂存区不一致）导致的 `git status` 误报。
- **结论/约定**：这类「大量 modified」在沙箱里是幻象，不要因此去 `git add -A` 或推送。真要核对以用户自己终端的 `git status` / `git fetch` 为准。

## 追加：基于代码实测修正「轻量」定位（本段）
- 用户问 MiniUnicorn 是否算轻量个人 AI 助手；我**未采信 README 自述**，直接量代码：Python 核心 65,518 行/224 文件，WebUI 源码 42,560 行/165 文件（排除 node_modules），全量≈108k 行/389 文件；编排核心(loop1575+runner1663+bus49)=3379 行=Python 总量 5.2%；依赖 31 个纯 Python 包、无 torch/numpy/langchain 等重型栈。
- **判断**：核心循环轻、依赖轻、单进程部署轻；但完整体量不轻（近 11 万行），「超轻量级」夸大。最准标签=「核心轻、外围重、依赖更轻」。
- 用户要求改 README 描述；**先按用户铁律(改动前先确认)给方案并 AskUserQuestion**，用户选「精准微调」后才动手。改动两处：①hero 第5行「超轻量级的个人 AI 代理框架」→「核心轻量、可审计、可扩展的开源个人 AI 代理框架」；②「这是什么」段末补一句体量的诚实说明（核心3.4k行/30依赖/单进程，全量~11万行非小脚本）。其余架构/依赖描述经核对与代码一致，未动。
- **已补全（用户后续指令）**：用户说「英文readme也修改一下」→ 改 README.en.md hero（`ultra-lightweight`→`lightweight at its core, auditable, and extensible`）+ "What is this" 段末补等量体量的英文说明；用户再说「都改掉」→ 改 `pyproject.toml` description=`A personal AI agent framework with a lightweight, auditable core`。至此中/英 README + 打包描述三处「轻量」口径完全对齐。
