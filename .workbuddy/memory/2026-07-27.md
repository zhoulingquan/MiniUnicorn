# 2026-07-27

## 任务：替换项目 Logo
- 用户提供一张新的独角兽 logo 图（透明背景的 JPG 文件，从剪贴板复制得到）。
- 项目里有 3 处 logo.svg 都内嵌着同一张 base64 PNG，需要全部替换：
  - `docs/logo.svg`（被 `README.md` 顶部引用）
  - `webui/public/logo.svg`（WebUI 公共资源，运行时被 `/logo.svg` 加载）
  - `miniunicorn/web/dist/logo.svg`（构建产物中的副本）
- 改动方式：把新图复制为同目录的 `logo.jpg`，再把 `logo.svg` 改为一个简单的 SVG 包装（`<image href="logo.jpg" .../>`）。这样：
  - 所有引用 `/logo.svg` 的地方（`webui/index.html`、`miniunicorn/web/dist/index.html`、`webui/src/components/thread/ThreadShell.tsx`）不用改；
  - README 的 `<img src="docs/logo.svg">` 也会自动跟随新图。
- 新增文件：`docs/logo.jpg`、`webui/public/logo.jpg`、`miniunicorn/web/dist/logo.jpg`（均 817 KB）。
- 修改文件：上面三个 `logo.svg`（每个 249 字节，viewBox 改为 1080×1080 适配新图比例）。
- 注意：`miniunicorn/web/dist/` 是构建输出，下次 `webui` rebuild 时会被 `webui/public/` 覆盖；不过因为两个源都已更新，效果会保持一致。
- **修复：自包含 SVG（base64 内嵌）**。第一版用 `<image href="logo.jpg">` 外链 jpg，在浏览器"SVG-as-img"安全模式下渲染不可靠，logo 实际不显示。改为直接把图片 base64 写进 SVG。三处 logo.svg 均已更新。
- **重要教训（用户纠正）**：用户明确指定的是 `miniunicorn-logo-original-transparent-4096.png`（透明 PNG）。但我第一次直接读了 `.workbuddy/clipboard-images/` 里的内容——剪贴板抓取会把 PNG 压成 JPEG（丢 alpha），于是误当成 RGB JPEG。正确做法：**用户指名了磁盘文件名时，要直接去磁盘找那个真文件**，而不是用剪贴板快照凑合。真原图在 `C:\Users\OOOH\Pictures\miniunicorn-logo-original-transparent-4096.png`（4096×4096 RGBA）。最终用该真·PNG resize 到 1024×1024、PNG optimize 后 base64 内嵌，三处 logo.svg 各 594KB，透明背景正确。

## 任务：启动项目（gateway）
- 标准启动命令 `miniunicorn gateway`，浏览器访问 http://127.0.0.1:8765 （WebSocket 也在同端口监听）。
- **正确的运行环境**：system Python 3.12（`C:\Users\OOOH\AppData\Local\Programs\Python\Python312\python.exe`）。
  - 该环境的 `miniunicorn` 是 editable 安装，import 路径指向 `D:\MyProject\MiniUnicorn\miniunicorn`（即项目源码），依赖齐全。
  - 启动命令：`python -m miniunicorn gateway`（cwd 设为项目根）。
- **踩坑记录（下次别再走弯路）**：
  1. managed Python 3.13 缺运行时依赖（import click 失败），不能直接 `python -m miniunicorn`，除非先 `pip install -e .`。
  2. `C:\...\Python312\Scripts\miniUnicorn.exe` 内部写死 `import miniUnicorn`（大写 U），与包名 `miniunicorn` 不符，直接运行报 `ModuleNotFoundError: No module named 'miniUnicorn'`，**不要直接用这个 exe**。
- gateway 启动后会自动初始化 `~/.miniUnicorn/` 配置与工作区；首次无 LLM Key 时对话不可用，需在 WebUI「设置 → 模型配置」填任意 OpenAI 兼容 Key（保存即生效，无需重启）。
- 本次已在后台启动（task_id: NsVMhE），首页 / 返回 200，`/logo.svg` 与 `/logo.jpg` 均正常服务（logo 替换已随启动生效）。

## 备注：两种运行模式端口（易混淆，已澄清）
- **8765** = `miniunicorn gateway`（生产式入口，服务 `miniunicorn/web/dist` 构建产物，含 WebSocket+HTTP）。这才是「启动项目」的方式，后台任务 NsVMhE 仍在运行，访问 http://127.0.0.1:8765。
- **5173** = webui 前端 Vite 开发服务器默认端口（`cd webui && npm run dev`），用于前端源码热更新开发。
- 已启动（2026-07-27 21:46，task_id: 9K8syE）：用 managed node 22.22.2 直接跑 `webui/node_modules/vite/bin/vite.js --host 127.0.0.1 --port 5173`（cwd=webui）。
  - 访问 http://127.0.0.1:5173 ；vite.config.ts 把 `/webui` `/api` `/auth` 代理到 8765 gateway，因此 **gateway(8765) 必须先于 5173 运行**。
  - 两者现在同时在线：8765=后端(PID 44028, task NsVMhE)，5173=前端(PID 25856, task 9K8syE)。
  - 验证：5173 首页 200、`/api` 经代理到 8765 返回 200、`/logo.svg` 200。

## 任务：修复 favicon 未跟随新 logo
- 用户反馈：favicon 没有正确用上新 logo（之前 favicon.svg 内嵌的 PNG 在 unicorn 脸部区域 alpha=0，显示为透明/黑色，与 `logo.svg` 的白脸独角兽不一致）。
- 修复方式：直接以 `C:\Users\OOOH\Pictures\miniunicorn-logo-original-transparent-4096.png`（4096×4096 RGBA 透明 PNG）为源，用 PIL LANCZOS 缩到 512×512、PNG optimize、再 base64 内嵌成 SVG（viewBox 0 0 512 512）。
- 同时更新两个 favicon.svg 副本：
  - `webui/public/favicon.svg`（开发源）
  - `miniunicorn/web/dist/favicon.svg`（构建产物副本，需随 webui rebuild 同步）
- 结果：face 区白色不透明像素占比 78.9%，与 logo.svg 的 79.5% 一致；视觉上 unicorn 白脸已正常显示。