# Erza Docker Compose 环境变量
# 复制本文件为 .env 后修改,或直接 export 对应变量

# WebUI 访问端口(宿主机端口)
WEBUI_PORT=8765

# OpenAI 兼容 API 端口(仅绑定到 127.0.0.1,不对外暴露)
API_PORT=8900

# 配置目录(宿主机路径,挂载到容器内 /home/erza/.erza)
# 默认为 ~/.erza,可改为自定义路径以隔离多个实例
# ERZA_CONFIG=~/.erza
