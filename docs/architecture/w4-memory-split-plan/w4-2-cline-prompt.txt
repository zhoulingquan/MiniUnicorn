# MiniUnicorn implementation task - Batch W4-2 (Consolidator -> memory_consolidator.py, pure-move refactor)

## Role and context

You are a senior Python implementation engineer on the MiniUnicorn project. The project's purpose: a business-intelligence agent for long-term use by a small enterprise (single user), with core demands of efficiency, leanness, and maintainability. This task is batch 2 of 3 of the memory.py split plan (W4 series; W4-1 is already committed as `20789f21`): implement independently, verify independently, commit independently, then STOP.

Working directory: D:\MyProject\MiniUnicorn

Prerequisite: the working tree must be clean apart from untracked temp/analysis dirs (`.tmp-*`, `.workbuddy/`, `arch-eval-*`, `docs/miniunicorn-architecture-analysis/`, `.trae-html-share-packages/`, `.tmp-w41-analysis.py`). If tracked files are modified, stop and report instead of stashing or resetting anything.

## Step 1: required reading (in order, before any implementation)

1. `docs/architecture/w4-memory-split-plan/00-overview.md` - plan overview (focus: section 4 test-impact surface, section 5 global constraints)
2. `docs/architecture/w4-memory-split-plan/02-w4-2-consolidator-split.md` - THE batch task book (sole implementation basis)

The task book is self-contained: refreshed current-state anchors (memory.py is 1129 lines after W4-1), an import-ownership table, the change plan, test requirements, do-not-touch list, acceptance checklist. Line numbers are reference values only - always locate by symbol. Your entire implementation scope is defined by the task book.

## Implementation discipline (takes priority over any efficiency concern)

### Four situations require immediate stop-and-report (never guess and continue)

1. Any "current-state anchor" in the task book does not match the actual code (symbol not found, structure mismatch)
2. Completing the task would require touching anything in the "do-not-touch list" - especially: if ANY architecture guard test fails (e.g. test_structured_memory_boundary.py), stop and report. The task book pre-verified that the Consolidator region has zero `StructuredMemoryRepository(` instantiations and zero `journal.jsonl` strings, so guards should NOT fire. A guard failure means you moved something into the wrong place - do NOT edit guard whitelists on your own initiative (the one whitelist edit in W4-1 was task-book-authorized; this batch has no such authorization)
3. Tests cannot pass and the cause is unclear - NEVER weaken assertions or delete/modify existing tests to make tests pass
4. Suspicious behavior not mentioned in the task book (suspected bug, suspected security hole) - record it in the report only, do NOT fix it

### Global red lines (full version in 00-overview.md section 5)

1. Semantics of W0-W3 protected achievements must not be touched. runner.py, loop.py, and memory_store.py get ZERO changes in this batch
2. **THIS BATCH IS A PURE-MOVE REFACTOR**: extract `Consolidator` + `_ARCHIVE_SUMMARY_MAX_CHARS` from memory.py (lines ~93-632) into a new file `miniunicorn/agent/memory_consolidator.py`. Zero behavior change. Move the class VERBATIM (whole contiguous region, including every docstring, Chinese comment, class attribute, the ratio asserts, and the WeakValueDictionary lock-pool comments). Do NOT move Dream, the four Dream module functions, or any MemoryStore code
3. **The runtime MemoryStore import is the critical invariant**: Consolidator.archive contains `MemoryStore._format_messages(messages)` (~line 373) - a RUNTIME class reference, not an annotation. The new module MUST import MemoryStore directly at runtime from memory_store.py (`from miniunicorn.agent.memory_store import MemoryStore, _RAW_ARCHIVE_MAX_CHARS`). Putting MemoryStore in a TYPE_CHECKING block will pass ruff AND collection but NameError at the first archive() call. Likewise `_RAW_ARCHIVE_MAX_CHARS` is a runtime value used in `_truncate_to_token_budget` - import it, never re-define it. Circular-import safety: memory_store.py does not import memory_consolidator, so this dependency is one-directional
4. **Facade compatibility**: after the move, `from miniunicorn.agent.memory import Consolidator` (and `_ARCHIVE_SUMMARY_MAX_CHARS`) must keep working - memory.py re-imports both from memory_consolidator.py with `# noqa: F401`. The eight consumer files (agent/__init__.py, loop.py, runtime_resources.py, dream_trigger.py, autocompact.py, context.py, command/memory.py, cli/_gateway_runner.py) must get ZERO changes - `git diff --stat` must not list them
5. **Monkeypatch target adjustments are the ONLY test changes allowed** (task book section 2.3, exactly 2 import lines covering 7 setattr sites): test_loop_consolidation_tokens.py patches `estimate_message_tokens` on the memory module namespace at 6 sites, and test_consolidation_ratio.py at 1 site. Both files' `memory_module` references are exactly the import line plus those setattr calls (verified), so changing ONLY the import line in each file (`import miniunicorn.agent.memory as memory_module` -> `import miniunicorn.agent.memory_consolidator as memory_module`) covers all 7 sites. The setattr calls, lambdas, token_map data, and assertions must NOT change. If any OTHER existing test needs modification, you moved something wrong: roll back and re-move
6. **Import trimming is ruff-driven, not memory-driven**: the new file imports only what its code references; remove now-unused imports from memory.py only when ruff reports them unused (shared names like call_purpose/render_template stay). The task book's import-ownership table is a pre-verified guide - trust ruff over the table
7. No new abstractions: no port/protocol/base class, no new config, no renaming, no visibility fixes for private cross-module access (`self.store._archive_identity` stays exactly as-is)
8. Use symbols for locating; line numbers in the task book are only reference values

### Git discipline

- One batch, one commit; NEVER `git add .`, only add explicit paths
- Never include `.tmp-*`, temp analysis scripts, or unrelated files
- Commit message: `refactor(w4-2): extract Consolidator into memory_consolidator.py (pure move)`

## Execution flow

1. Read both files -> check each "current-state anchor" in the task book against the actual code, record the conclusion
2. If consistent -> implement strictly per the task book's "changes" section: create memory_consolidator.py (docstring + imports + runtime MemoryStore/_RAW_ARCHIVE_MAX_CHARS import + _ARCHIVE_SUMMARY_MAX_CHARS + Consolidator verbatim), then shrink memory.py (delete lines ~93-632 content i.e. the constant and the class, add the facade re-import block, trim only ruff-flagged unused imports), then apply the 2 monkeypatch import-line adjustments
3. Implement ALL new tests in the task book's section 2.4 (append to tests/agent/test_memory_module_split.py: facade identity, shared-constant identity, archive-summary identity, line-count < 620; the 5th optional case only if cheaply constructible)
4. Verification gate (all must pass before commit is allowed) - MUST use the project venv Python 3.12:
   - `.venv\Scripts\python.exe -m pytest tests/ -q` -> 0 failed, passed >= 4119
   - `.venv\Scripts\python.exe -m ruff check miniunicorn/` -> zero output
   - `.venv\Scripts\python.exe -m ruff format --check miniunicorn/` -> zero output
   - WARNING: do not use the system default python (3.10, missing typing.Self, causes 14 ImportErrors at collection - that is an environment problem, not a code problem)
   - The full pytest run takes about 9-13 minutes. Run it in the background with output redirected to a file under $env:TEMP, then poll with long sleep intervals (25s+); do NOT give up on the full run. If exactly one unrelated timing-sensitive test fails, rerun that single test in isolation to confirm flakiness, then rerun the full suite once; report honestly either way
5. As soon as the verification gate passes, `git add` the explicit paths (expected: `miniunicorn/agent/memory_consolidator.py`, `miniunicorn/agent/memory.py`, `tests/agent/test_loop_consolidation_tokens.py`, `tests/agent/test_consolidation_ratio.py`, `tests/agent/test_memory_module_split.py`) and COMMIT IMMEDIATELY - commit first, then write the report. This ordering matters: if your session gets interrupted, the completed work must already be safely committed
6. Output the batch report (format below)
7. STOP after completion: do not start W4-3 or anything else, end without waiting for further questions

If you cannot finish everything in one run: clearly report how far you got; if the verification gate has not passed, do not commit; do not leave an unclear half-finished state (always roll back half-finished work with `git checkout -- <files>` before reporting).

## Batch report format (output last)

## Batch report: W4-2
1. Anchor check: all consistent / deviation on item N: <explanation>
2. Changed files: <list each>
3. New tests: <file names + case count>
4. Verification gate: pytest <N passed / 0 failed>; ruff <zero warnings>
5. Deviations from the task book: listed one by one / none
6. Acceptance checklist: <check each item in the task book's final section, explain any not met>
7. Incidental findings (recorded only, not acted on): <problem list / none>
