# MiniUnicorn implementation task - Batch W4-1 (MemoryStore -> memory_store.py, pure-move refactor)

## Role and context

You are a senior Python implementation engineer on the MiniUnicorn project. The project's purpose: a business-intelligence agent for long-term use by a small enterprise (single user), with core demands of efficiency, leanness, and maintainability. This task is batch 1 of 3 of the memory.py split plan (W4 series): implement independently, verify independently, commit independently, then STOP.

Working directory: D:\MyProject\MiniUnicorn

Prerequisite: tag `baseline-pre-w4` exists (HEAD is the docs commit of the W4 plan; the code base equals W3-2's `e5d62312`). The working tree must be clean apart from untracked temp/analysis dirs (`.tmp-*`, `.workbuddy/`, `arch-eval-*`, `docs/miniunicorn-architecture-analysis/`, `.trae-html-share-packages/`). If tracked files are modified, stop and report instead of stashing or resetting anything.

## Step 1: required reading (in order, before any implementation)

1. `docs/architecture/w4-memory-split-plan/00-overview.md` - plan overview (focus: section 2 verdict table, section 4 test-impact surface, section 5 global constraints)
2. `docs/architecture/w4-memory-split-plan/01-w4-1-memory-store-split.md` - THE batch task book (sole implementation basis)

The task book is self-contained: current-state anchors, change plan, test requirements, do-not-touch list, acceptance checklist. Line numbers in the task book are reference values only - always locate by symbol. Your entire implementation scope is defined by the task book.

## Implementation discipline (takes priority over any efficiency concern)

### Four situations require immediate stop-and-report (never guess and continue)

1. Any "current-state anchor" in the task book does not match the actual code (symbol not found, structure mismatch)
2. Completing the task would require touching anything in the "do-not-touch list" (runner.py, loop.py, the consumer files' import statements, Collaborator modules)
3. Tests cannot pass and the cause is unclear - NEVER weaken assertions or delete/modify existing tests to make tests pass
4. Suspicious behavior not mentioned in the task book (suspected bug, suspected security hole) - record it in the report only, do NOT fix it

### Global red lines (full version in 00-overview.md section 5)

1. Semantics of W0/W1/W2/W3 protected achievements must not be touched: trusted evidence pipeline, three-state approval gate, W1-2 injection-fallback dual path, W2-2 runner phase split, W3 loop phase splits. runner.py and loop.py get ZERO changes in this batch
2. **THIS BATCH IS A PURE-MOVE REFACTOR**: extract `MemoryStore` + `WorkspaceMemoryRegistry` + two shared constants from memory.py into a new file `miniunicorn/agent/memory_store.py`. Zero behavior change. Move the two classes VERBATIM (whole contiguous regions, including every docstring, Chinese comment, and blank-line structure). Do NOT change any condition, log text, lock order, cache invalidation, or cursor semantics. Do NOT move the four Dream module functions, `_ARCHIVE_SUMMARY_MAX_CHARS`, Consolidator, or Dream
3. **Facade compatibility is the core acceptance criterion**: after the move, `from miniunicorn.agent.memory import MemoryStore, WorkspaceMemoryRegistry, _HISTORY_ENTRY_HARD_CAP, _RAW_ARCHIVE_MAX_CHARS` must keep working (memory.py re-imports them from memory_store.py with `# noqa: F401`). The eight consumer files (agent/__init__.py, loop.py, runtime_resources.py, dream_trigger.py, autocompact.py, context.py, command/memory.py, cli/_gateway_runner.py) must get ZERO changes - `git diff --stat` must not list them
4. **Monkeypatch target adjustments are the ONLY test changes allowed** (task book section 2.3, exactly 2 sites): test_memory_store.py patches `migrate_legacy_journal` on the memory module namespace, and test_reflection_structured.py patches `_atomic_rewrite_lines` on the memory module namespace - after the move, MemoryStore resolves those names in the memory_store.py namespace, so those two test files' module import must change from `miniunicorn.agent.memory` to `miniunicorn.agent.memory_store`. The monkeypatch calls themselves, their assertions, and their lambda/spy bodies must NOT change. These are pure import-path adjustments, NOT assertion changes. If any OTHER existing test needs modification, you moved something wrong: roll back and re-move
5. **Shared constant ownership**: `_RAW_ARCHIVE_MAX_CHARS` (used by MemoryStore at ~line 902 AND by Consolidator at ~line 1239) and `_HISTORY_ENTRY_HARD_CAP` move to memory_store.py, which is their single definition site. `_ARCHIVE_SUMMARY_MAX_CHARS` (Consolidator-only) STAYS in memory.py for the next batch. memory.py's re-import block keeps Consolidator's `_RAW_ARCHIVE_MAX_CHARS` reference working
6. **Import trimming is ruff-driven, not memory-driven**: the new file's import block contains only names its code actually references; remove now-unused imports from memory.py only when ruff reports them as unused. Do not guess
7. No new abstractions: no port/protocol/base class, no new config, no renaming, no visibility fixes for private cross-module access (`store._archive_identity` style access stays exactly as-is - that is W4-2 territory anyway)
8. Use symbols for locating; line numbers in the task book are only reference values

### Git discipline

- One batch, one commit; NEVER `git add .`, only add explicit paths
- Never include `.tmp-*`, temp reports, or unrelated files
- Commit message: `refactor(w4-1): extract MemoryStore and WorkspaceMemoryRegistry into memory_store.py`

## Execution flow

1. Read both files -> check each "current-state anchor" in the task book against the actual code, record the conclusion
2. If consistent -> implement strictly per the task book's "changes" section: create memory_store.py (docstring + trimmed imports + MemoryStore verbatim + WorkspaceMemoryRegistry verbatim + the two constants), then shrink memory.py (delete the two classes and two constants, add the facade re-import block with `# noqa: F401`, trim only ruff-flagged unused imports), then apply the 2 monkeypatch target adjustments
3. Implement ALL new tests in the task book's section 2.4 (test_memory_module_split.py, 5 cases: facade identity x2 classes, standalone import, constants identity, agent-package re-export identity, line-count structure assertion)
4. Verification gate (all must pass before commit is allowed) - MUST use the project venv Python 3.12:
   - `.venv\Scripts\python.exe -m pytest tests/ -q` -> 0 failed, passed >= 4119
   - `.venv\Scripts\python.exe -m ruff check miniunicorn/` -> zero output
   - `.venv\Scripts\python.exe -m ruff format --check miniunicorn/` -> zero output
   - WARNING: do not use the system default python (3.10, missing typing.Self, causes 14 ImportErrors at collection - that is an environment problem, not a code problem)
   - The full pytest run takes about 8-13 minutes. Run it in the background with output redirected to a file under $env:TEMP, then poll with long sleep intervals (30s+); do NOT give up on the full run. If exactly one unrelated timing-sensitive test fails, rerun that single test in isolation to confirm flakiness, then rerun the full suite once; report honestly either way
5. As soon as the verification gate passes, `git add` the explicit paths (expected: `miniunicorn/agent/memory_store.py`, `miniunicorn/agent/memory.py`, `tests/agent/test_memory_store.py`, `tests/agent/test_reflection_structured.py`, `tests/agent/test_memory_module_split.py`) and COMMIT IMMEDIATELY - commit first, then write the report. This ordering matters: if your session gets interrupted, the completed work must already be safely committed
6. Output the batch report (format below)
7. STOP after completion: do not start W4-2 or anything else, end without waiting for further questions

If you cannot finish everything in one run: clearly report how far you got; if the verification gate has not passed, do not commit; do not leave an unclear half-finished state (always roll back half-finished work with `git checkout -- <files>` before reporting).

## Batch report format (output last)

## Batch report: W4-1
1. Anchor check: all consistent / deviation on item N: <explanation>
2. Changed files: <list each>
3. New tests: <file names + case count>
4. Verification gate: pytest <N passed / 0 failed>; ruff <zero warnings>
5. Deviations from the task book: listed one by one / none
6. Acceptance checklist: <check each item in the task book's final section, explain any not met>
7. Incidental findings (recorded only, not acted on): <problem list / none>
