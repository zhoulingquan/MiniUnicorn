# MiniUnicorn implementation task - Batch W4-3 (Dream -> memory_dream.py + pure facade, pure-move refactor)

## Role and context

You are a senior Python implementation engineer on the MiniUnicorn project. The project's purpose: a business-intelligence agent for long-term use by a small enterprise (single user), with core demands of efficiency, leanness, and maintainability. This task is the FINAL batch of the memory.py split plan (W4 series; W4-1 is `20789f21`, W4-2 is `2b322b9d`): implement independently, verify independently, commit independently, then STOP.

Working directory: D:\MyProject\MiniUnicorn

Prerequisite: the working tree must be clean apart from untracked temp/analysis dirs (`.tmp-*`, `.workbuddy/`, `arch-eval-*`, `docs/miniunicorn-architecture-analysis/`, `.trae-html-share-packages/`, `.tmp-w41-analysis.py`, `.tmp-w4*-analysis*`). If tracked files are modified, stop and report instead of stashing or resetting anything.

## Step 1: required reading (in order, before any implementation)

1. `docs/architecture/w4-memory-split-plan/00-overview.md` - plan overview (focus: section 5 global constraints)
2. `docs/architecture/w4-memory-split-plan/03-w4-3-dream-split.md` - THE batch task book (sole implementation basis)

The task book is self-contained: refreshed current-state anchors (memory.py is 584 lines after W4-2), a verified import-ownership table, the change plan, test requirements, do-not-touch list, acceptance checklist. Line numbers are reference values only - always locate by symbol. Your entire implementation scope is defined by the task book.

## Implementation discipline (takes priority over any efficiency concern)

### Four situations require immediate stop-and-report (never guess and continue)

1. Any "current-state anchor" in the task book does not match the actual code (symbol not found, structure mismatch)
2. Completing the task would require touching anything in the "do-not-touch list" - especially: if ANY architecture guard test fails (e.g. test_structured_memory_boundary.py), stop and report. The task book pre-verified the Dream region has zero legacy-journal strings and zero `StructuredMemoryRepository(` instantiations, so guards should NOT fire. A guard failure means you moved something wrong - do NOT edit guard whitelists or guard logic on your own initiative
3. Tests cannot pass and the cause is unclear - NEVER weaken assertions or delete/modify existing tests to make tests pass
4. Suspicious behavior not mentioned in the task book (suspected bug, suspected security hole) - record it in the report only, do NOT fix it

### Global red lines (full version in 00-overview.md section 5)

1. Semantics of W0-W3 protected achievements must not be touched. runner.py, loop.py, memory_store.py, and memory_consolidator.py get ZERO changes in this batch
2. **THIS BATCH IS A PURE-MOVE REFACTOR**: extract the four Dream module functions (`_parse_datetime_loose`, `reflection_evidence_id`, `_dream_source_batch`, `count_pending_dream_entries`) and `class Dream` (~496 lines, lines 39-584) from memory.py into a new file `miniunicorn/agent/memory_dream.py`, then shrink memory.py to a pure facade (~40 lines of re-imports). Zero behavior change. Move everything VERBATIM including every docstring, Chinese comment, class attribute, and the nested functions (`render`, `take_history`, `take_reflections`). The three `store._export_audit_pending()` calls must land in memory_dream.py exactly as-is
3. **Import ownership is pre-verified (trust ruff over the table)**: memory_dream.py needs at runtime: `hashlib`, `re`, `dataclasses_replace`, `datetime`/`timezone`, `logger` (loguru), `CallPurpose`/`call_purpose`, `session_key_base`, `estimate_message_tokens` (6 call sites in `_fit_bounded_batch`, ~lines 273-300), `render_template`; TYPE_CHECKING-only: `MemoryStatus`, `LLMProvider`, `MemoryStore`. CRITICAL DIFFERENCE FROM W4-2: Dream has NO runtime MemoryStore reference (only a string annotation in `count_pending_dream_entries`) - so MemoryStore goes in the TYPE_CHECKING block. Putting it at runtime would still work (no cycle: memory_store does not import memory_dream) but TYPE_CHECKING is the verbatim-equivalent choice
4. **Facade compatibility is the core acceptance criterion**: after the move, `from miniunicorn.agent.memory import <any of the 11 symbols>` must keep working. memory.py becomes EXACTLY: one docstring line + four `# noqa: F401` re-import blocks (from memory_consolidator: `Consolidator`, `_ARCHIVE_SUMMARY_MAX_CHARS`; from memory_dream: `Dream`, `count_pending_dream_entries`, `reflection_evidence_id`, `_dream_source_batch`, `_parse_datetime_loose`; from memory_jsonl_import: `LegacyJournalImportError`, `migrate_legacy_journal`; from memory_store: `MemoryStore`, `WorkspaceMemoryRegistry`, `_HISTORY_ENTRY_HARD_CAP`, `_RAW_ARCHIVE_MAX_CHARS`) - the full block is in the task book section 2.2. NO other code in the facade. The jsonl_import forward RESTORES the baseline namespace (ruff removed it in W4-1 as unused); it is authorized, not new surface
5. **The eight consumer files get ZERO changes** (agent/__init__.py, loop.py, runtime_resources.py, dream_trigger.py, autocompact.py, context.py, command/memory.py, cli/_gateway_runner.py) - `git diff --stat` must not list them. Existing test imports (`from miniunicorn.agent.memory import Dream, MemoryStore, ...` in test_dream_structured_memory.py, test_dream_trigger.py, test_reflection_structured.py, including three function-local `_dream_source_batch` imports at ~519/526/959) must keep working unchanged
6. **ZERO existing test modifications this batch** - this is the simplest batch on the test side (pre-verified: no namespace-level monkeypatch on Dream symbols; the monkeypatch adjustments were completed in W4-1/W4-2). If ANY existing test needs a change, you moved something wrong: roll back and re-move
7. **Two authorized comment/namespace deviations** (expected, list them in the report): (a) the stale `# MemoryStore — pure file I/O layer` banner at lines 34-36 is DELETED (not carried into memory_dream.py) - it describes a class that moved out in W4-1; (b) the jsonl_import forward in the facade (see red line 4). Nothing else may deviate
8. No new abstractions, no renaming, no visibility fixes for private cross-module access

### Git discipline

- One batch, one commit; NEVER `git add .`, only add explicit paths
- Never include `.tmp-*`, temp analysis scripts, or unrelated files
- Commit message: `refactor(w4-3): extract Dream into memory_dream.py, memory.py becomes pure facade (pure move)`

## Execution flow

1. Read both files -> check each "current-state anchor" in the task book against the actual code, record the conclusion
2. If consistent -> implement strictly per the task book's "changes" section: create memory_dream.py (docstring + imports + four functions verbatim + Dream banner + class Dream verbatim), then shrink memory.py to the pure facade (delete lines ~34-584 content and now-unused imports, write the four re-import blocks, update the docstring to the facade description), then verify the consumer files are untouched
3. Implement ALL 5 new tests in the task book's section 2.4 (append to tests/agent/test_memory_module_split.py): final facade identity (11 symbols), pure-facade structure (<= 60 lines, no `class `), jsonl re-export identity, dream helper smoke, consumer entry points
4. Verification gate (all must pass before commit is allowed) - MUST use the project venv Python 3.12:
   - `.venv\Scripts\python.exe -m pytest tests/ -q` -> 0 failed, passed >= 4124
   - `.venv\Scripts\python.exe -m ruff check miniunicorn/` -> zero output
   - `.venv\Scripts\python.exe -m ruff format --check miniunicorn/` -> zero output
   - WARNING: do not use the system default python (3.10, missing typing.Self, causes 14 ImportErrors at collection - that is an environment problem, not a code problem)
   - The full pytest run takes about 7-11 minutes. Launch it as a DETACHED background process (`Start-Process -FilePath ".venv\Scripts\python.exe" -ArgumentList "-m","pytest","tests/","-q" -RedirectStandardOutput "$env:TEMP\w43_pytest.log" -RedirectStandardError "$env:TEMP\w43_pytest_err.log" -NoNewWindow -PassThru`) so it survives a session interruption, then poll the log with long sleep intervals (25s+); do NOT give up on the full run. If exactly one unrelated timing-sensitive test fails, rerun that single test in isolation to confirm flakiness, then rerun the full suite once; report honestly either way
5. As soon as the verification gate passes, `git add` the explicit paths (expected: `miniunicorn/agent/memory_dream.py`, `miniunicorn/agent/memory.py`, `tests/agent/test_memory_module_split.py`) and COMMIT IMMEDIATELY - commit first, then write the report. This ordering matters: if your session gets interrupted, the completed work must already be safely committed
6. Output the batch report (format below)
7. STOP after completion: do not start anything else, end without waiting for further questions

If you cannot finish everything in one run: clearly report how far you got; if the verification gate has not passed, do not commit; do not leave an unclear half-finished state (always roll back half-finished work with `git checkout -- <files>` before reporting).

## Batch report format (output last)

## Batch report: W4-3
1. Anchor check: all consistent / deviation on item N: <explanation>
2. Changed files: <list each>
3. New tests: <file names + case count>
4. Verification gate: pytest <N passed / 0 failed>; ruff <zero warnings>
5. Deviations from the task book: listed one by one (the 2 authorized deviations count) / none
6. Acceptance checklist: <check each item in the task book's final section, explain any not met>
7. Incidental findings (recorded only, not acted on): <problem list / none>
